# Lilies for Valentine's

A tiny React page that asks a sweet question and runs locally.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:5173

## Share on the same Wi-Fi

```bash
npm run dev -- --host
```

Copy the Network URL shown in the terminal and send it to the other person.
